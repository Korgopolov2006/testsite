# 🔑 Инструкция по авторизации за разные роли

## Администратор
**Логин:** `admin`  
**Пароль:** `admin` (или тот, что вы установили при создании суперпользователя)

### Как создать суперпользователя:
```bash
python manage.py createsuperuser --username admin --email admin@zhevzhik.ru
```

### Доступ к админ-панели:
- URL: http://127.0.0.1:8000/admin/
- Полные права на все разделы
- Может создавать и редактировать пользователей
- Может назначать роли

## Создание пользователей с разными ролями

### 1. Через админ-панель:
1. Войдите как администратор: http://127.0.0.1:8000/admin/
2. Перейдите в раздел "Пользователи" → "Пользователи"
3. Нажмите "Добавить пользователя"
4. Заполните данные:
   - **Username:** `manager1` (или любое другое имя)
   - **Email:** `manager@zhevzhik.ru`
   - **Пароль:** `manager123`
   - **Роль:** Выберите из списка (Менеджер магазина, Курьер, Покупатель)
   - **Имя:** `Иван`
   - **Фамилия:** `Менеджеров`
   - **Телефон:** `+79991234567`

### 2. Через Django shell:
```bash
python manage.py shell
```

```python
from paint_shop_project.models import User, Role

# Создаем роли (если их нет)
admin_role, _ = Role.objects.get_or_create(name='admin')
manager_role, _ = Role.objects.get_or_create(name='manager')
courier_role, _ = Role.objects.get_or_create(name='courier')
customer_role, _ = Role.objects.get_or_create(name='customer')

# Создаем менеджера
manager = User.objects.create_user(
    username='manager1',
    email='manager@zhevzhik.ru',
    password='manager123',
    first_name='Иван',
    last_name='Менеджеров',
    phone='+79991234567',
    role=manager_role
)

# Создаем курьера
courier = User.objects.create_user(
    username='courier1',
    email='courier@zhevzhik.ru',
    password='courier123',
    first_name='Петр',
    last_name='Курьеров',
    phone='+79991234568',
    role=courier_role
)

# Создаем покупателя
customer = User.objects.create_user(
    username='customer1',
    email='customer@zhevzhik.ru',
    password='customer123',
    first_name='Анна',
    last_name='Покупателева',
    phone='+79991234569',
    role=customer_role
)
```

## Доступные роли:

### 👑 Администратор (`admin`)
- Полный доступ ко всем функциям
- Управление пользователями
- Настройка системы
- Просмотр аналитики

### 🏪 Менеджер магазина (`manager`)
- Управление товарами
- Просмотр заказов
- Управление акциями
- Работа с клиентами

### 🚚 Курьер (`courier`)
- Просмотр заказов на доставку
- Отметка о доставке
- Просмотр маршрутов

### 🛒 Покупатель (`customer`)
- Просмотр товаров
- Добавление в корзину
- Оформление заказов
- Просмотр истории

## Тестирование ролей:

1. **Войдите как администратор:** http://127.0.0.1:8000/admin/
2. **Создайте пользователей** с разными ролями
3. **Выйдите** из админ-панели
4. **Войдите через обычную форму** или **по телефону** с созданными учетными данными

## Авторизация по телефону:

1. Перейдите на: http://127.0.0.1:8000/phone-login/
2. Введите номер телефона пользователя
3. Получите SMS-код (в консоли)
4. Введите код для входа

## Примечания:

- SMS-коды генерируются в консоли (для разработки)
- В продакшене нужно подключить реальный SMS-провайдер
- Все пароли можно изменить через админ-панель
- Роли можно изменять в профиле пользователя

