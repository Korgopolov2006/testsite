# ⚡ Быстрый деплой - 5 минут до запуска в интернете

## 🎯 Самый простой способ - Railway.app

### Шаг 1: Подготовка кода (уже сделано!)
✅ Все файлы конфигурации готовы
✅ `requirements.txt` обновлен
✅ `settings.py` настроен для production
✅ Создан `.gitignore`

### Шаг 2: Загрузите код на GitHub

1. Создайте репозиторий на [GitHub.com](https://github.com/new)
2. Выполните команды в папке проекта:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/ваш-repo.git
git push -u origin main
```

### Шаг 3: Деплой на Railway

1. **Зарегистрируйтесь:** [railway.app](https://railway.app/) (можно через GitHub)

2. **Создайте проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Добавьте базу данных:**
   - В проекте нажмите "+ New"
   - Выберите "Database" → "PostgreSQL"
   - Railway автоматически создаст базу данных

4. **Настройте переменные окружения:**
   
   Нажмите на ваш сервис → "Variables" → добавьте:
   
   ```
   SECRET_KEY=вставьте-сюда-случайную-строку-50-символов
   DEBUG=False
   ALLOWED_HOSTS=${{RAILWAY_PUBLIC_DOMAIN}}
   DATABASE_URL=${{Postgres.DATABASE_URL}}
   CSRF_TRUSTED_ORIGINS=https://${{RAILWAY_PUBLIC_DOMAIN}}
   ```

   **Как сгенерировать SECRET_KEY:**
   ```python
   # Выполните в Python консоли:
   from django.core.management.utils import get_random_secret_key
   print(get_random_secret_key())
   ```

5. **Деплой:**
   - Railway автоматически начнет деплой
   - Подождите 2-3 минуты
   - Ваш сайт будет доступен по адресу: `https://ваш-проект.railway.app`

### Шаг 4: Создайте суперпользователя

В Railway Dashboard:
1. Откройте ваш сервис
2. Перейдите в "Deployments"
3. Нажмите на текущий деплой
4. Откройте "Terminal" tab (или в самом деплое есть кнопка "View Logs")
5. В Settings найдите "Command" и введите:

```bash
python manage.py createsuperuser
```

### Шаг 5: Готово! 🎉

Ваш сайт работает в интернете!

- **Сайт:** `https://ваш-проект.railway.app`
- **Админка:** `https://ваш-проект.railway.app/admin`
- **API:** `https://ваш-проект.railway.app/api/`
- **Swagger:** `https://ваш-проект.railway.app/swagger/`

---

## 🔄 Альтернатива: Render.com

Если Railway не подходит, используйте Render.com:

1. **Зарегистрируйтесь:** [render.com](https://render.com/)

2. **Создайте Web Service:**
   - "New +" → "Web Service"
   - Подключите GitHub репозиторий
   
3. **Настройки:**
   - **Name:** любое имя
   - **Build Command:** `pip install -r requirements.txt && python manage.py collectstatic --noinput`
   - **Start Command:** `gunicorn paint_shop.wsgi:application`
   - **Environment:** Python 3

4. **Добавьте PostgreSQL:**
   - "New +" → "PostgreSQL"
   - Скопируйте "Internal Database URL"

5. **Переменные окружения:**
   ```
   SECRET_KEY=ваш-секретный-ключ
   DEBUG=False
   DATABASE_URL=вставьте-database-url-отсюда
   ALLOWED_HOSTS=ваш-сайт.onrender.com
   CSRF_TRUSTED_ORIGINS=https://ваш-сайт.onrender.com
   ```

6. **Deploy** - и готово!

---

## 📱 Что дальше?

### Импортировать продукты:
```bash
python manage.py import_products
python manage.py seed_demo_data
```

### Настроить Telegram бот (опционально):
1. Создайте бота через [@BotFather](https://t.me/botfather)
2. Добавьте переменную: `TELEGRAM_BOT_TOKEN=ваш-токен`

### Подключить свой домен:
- Railway: Settings → Domains → Add custom domain
- Render: Settings → Custom Domain

---

## ❓ Проблемы?

### Ошибка миграций:
```bash
python manage.py migrate
```

### Статика не загружается:
```bash
python manage.py collectstatic --noinput
```

### Проверить логи:
- Railway: Deployments → View Logs
- Render: Logs tab

---

## 💰 Стоимость

- **Railway:** $5/месяц бесплатно (хватит для старта)
- **Render:** Бесплатный план (сайт засыпает после 15 мин неактивности)
- **Heroku:** $5/месяц минимум

---

## 🔒 Важно!

✅ **НЕ коммитьте** файл `.env` в Git (уже в `.gitignore`)
✅ **Смените** `SECRET_KEY` на уникальный
✅ **Установите** `DEBUG=False` в production
✅ **Используйте** HTTPS (автоматически на Railway/Render)

---

**Готово! Теперь ваш сайт доступен всему миру! 🌍**
