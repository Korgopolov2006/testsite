# 🚨 Настройка Alert Rules в Grafana

## 📊 Выбранная метрика для мониторинга

### Рекомендуемая метрика: `zhevzhik_orders_today`

**Почему эта метрика:**
- ✅ Бизнес-критичная метрика (показывает активность клиентов)
- ✅ Легко понять и интерпретировать
- ✅ Можно легко изменить для демонстрации (добавить/удалить заказы)
- ✅ Показывает реальную работу приложения

**Альтернативные метрики:**
- `zhevzhik_http_errors_total` - для мониторинга ошибок
- `zhevzhik_users_active` - для мониторинга пользователей
- `zhevzhik_revenue_today` - для мониторинга выручки

---

## 🎯 Сценарий демонстрации

### Alert Rule 1: Высокое количество заказов
- **Название**: "Высокое количество заказов сегодня"
- **Условие**: `zhevzhik_orders_today > 50`
- **For**: 5 минут (Pending)
- **Severity**: warning

### Alert Rule 2: Низкое количество заказов
- **Название**: "Низкое количество заказов сегодня"
- **Условие**: `zhevzhik_orders_today < 5`
- **For**: 10 минут (Pending)
- **Severity**: info

### Alert Rule 3: HTTP ошибки (бонус)
- **Название**: "Высокое количество HTTP ошибок"
- **Условие**: `rate(zhevzhik_http_errors_total[5m]) > 0.1`
- **For**: 2 минуты (Pending)
- **Severity**: critical

---

## 📋 Пошаговая инструкция

### Шаг 1: Создание Alert Rule в Grafana

1. **Откройте Grafana**: http://localhost:3000
2. **Перейдите в Alerting**:
   - Нажмите на иконку ⚡ (Alerting) в левом меню
   - Или перейдите: Alerting → Alert rules

3. **Создайте новую Alert Rule**:
   - Нажмите **"New alert rule"**
   - Или нажмите **"Import"** и загрузите `grafana_alert_rules.json`

### Шаг 2: Ручная настройка Alert Rule

#### 2.1. Основные настройки

**Name**: `Высокое количество заказов сегодня`

**Folder**: Выберите папку для алертов (можно создать новую)

**Evaluation interval**: `30s` (как часто проверять)

#### 2.2. Настройка Query (Query A)

**Data source**: `Prometheus` (ваш Prometheus data source)

**Query**:
```promql
zhevzhik_orders_today
```

**Legend**: `Заказов сегодня`

**Time range**: `Last 5 minutes`

#### 2.3. Настройка Condition (Query B)

**Type**: `Math`

**Expression**:
```
$A > 50
```

Это означает: если значение из Query A больше 50, условие выполнено.

#### 2.4. Настройка Alert Rule (Query C)

**Type**: `Threshold`

**Condition**: `C` (результат из Query B)

**For**: `5 minutes` (период ожидания перед Firing)

**No data**: `NoData` (что делать, если нет данных)

**Error handling**: `Alerting` (что делать при ошибке)

#### 2.5. Настройка Annotations

**Summary**:
```
Количество заказов сегодня: {{ $values.A }}
```

**Description**:
```
Обнаружено высокое количество заказов за сегодня: {{ $values.A }}.
Порог: 50 заказов.
Время: {{ $time }}
```

#### 2.6. Настройка Labels

Добавьте labels для фильтрации:

- **severity**: `warning`
- **team**: `business`
- **metric**: `orders_today`

#### 2.7. Сохранение

- Нажмите **"Save rule"**
- Alert Rule создана!

---

## 🎬 Демонстрация работы Alert Rule

### Сценарий 1: Нормальное состояние (Normal)

**Текущее значение**: `zhevzhik_orders_today = 10`

1. Проверьте статус в Grafana:
   - Alerting → Alert rules
   - Статус: **Normal** (зелёный)

2. Проверьте панель:
   - Создайте панель с метрикой `zhevzhik_orders_today`
   - Значение должно быть 10

### Сценарий 2: Переход в Pending

**Действие**: Увеличьте количество заказов до 55

1. **В Django Admin**:
   - Создайте несколько заказов через админку
   - Или используйте shell:
   ```python
   python manage.py shell
   >>> from paint_shop_project.models import Order
   >>> # Создайте заказы до превышения порога 50
   ```

2. **Обновите метрику**:
   ```python
   >>> from paint_shop_project.prometheus_metrics import update_business_metrics
   >>> update_business_metrics()
   ```

3. **Проверьте в Grafana**:
   - Alerting → Alert rules
   - Статус: **Pending** (жёлтый)
   - Время до Firing: уменьшается (осталось ~5 минут)

### Сценарий 3: Переход в Firing

**Ожидание**: Подождите 5 минут (пока значение > 50)

1. **Проверьте статус**:
   - Alerting → Alert rules
   - Статус: **Firing** (красный) 🔴

2. **Проверьте Alert**:
   - Alerting → Alerts
   - Должен появиться новый alert со статусом **Firing**

3. **Проверьте уведомления**:
   - Если настроены уведомления (email, Slack и т.д.), они должны быть отправлены

### Сценарий 4: Переход в Resolved

**Действие**: Уменьшите количество заказов до 40

1. **В Django Admin**:
   - Удалите несколько заказов
   - Или измените дату создания заказов

2. **Обновите метрику**:
   ```python
   >>> update_business_metrics()
   ```

3. **Проверьте статус**:
   - Alerting → Alert rules
   - Статус: **Normal** (зелёный) ✅
   - Alert → Alerts: статус изменился на **Resolved**

4. **Проверьте историю**:
   - Alerting → Alert history
   - Должна быть запись о переходе из Firing в Resolved

---

## 📊 Создание панели для мониторинга

### Панель для отслеживания метрики

1. **Создайте новый Dashboard**

2. **Добавьте панель**:
   - Type: **Stat** или **Time series**
   - Title: `Заказов сегодня`

3. **Query**:
   ```promql
   zhevzhik_orders_today
   ```

4. **Добавьте визуализацию Alert**:
   - Включите опцию **"Show alert state"**
   - Это покажет, когда alert активен

5. **Добавьте пороги**:
   - Thresholds:
     - Green: `0 - 50`
     - Yellow: `50 - 100`
     - Red: `100+`

---

## 🧪 Тестирование с помощью Python скрипта

Создайте файл `test_alert_metric.py`:

```python
#!/usr/bin/env python
"""Скрипт для тестирования метрики заказов"""
import os
import django
import time

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'paint_shop.settings')
django.setup()

from paint_shop_project.models import Order
from paint_shop_project.prometheus_metrics import update_business_metrics
from django.utils import timezone
from decimal import Decimal

def test_alert_scenarios():
    """Тестирование различных сценариев alert"""
    
    print("=" * 60)
    print("Тестирование Alert Rules")
    print("=" * 60)
    
    # Сценарий 1: Нормальное состояние
    print("\n1. Нормальное состояние (10 заказов)")
    # Убедитесь, что сегодня есть 10 заказов
    update_business_metrics()
    print("   Метрика обновлена. Проверьте в Grafana: статус Normal")
    input("   Нажмите Enter для продолжения...")
    
    # Сценарий 2: Превышение порога
    print("\n2. Превышение порога (55 заказов)")
    print("   Создаём дополнительные заказы...")
    
    # Создайте заказы для превышения порога
    # (адаптируйте под вашу модель Order)
    for i in range(45):
        try:
            Order.objects.create(
                order_date=timezone.now(),
                status='created',
                # ... другие обязательные поля
            )
        except Exception as e:
            print(f"   Ошибка создания заказа: {e}")
    
    update_business_metrics()
    print("   Метрика обновлена. Проверьте в Grafana: статус Pending")
    print("   Подождите 5 минут для перехода в Firing")
    input("   Нажмите Enter для продолжения...")
    
    # Сценарий 3: Возврат к нормальному состоянию
    print("\n3. Возврат к нормальному состоянию")
    print("   Удаляем лишние заказы...")
    
    # Удалите заказы, созданные сегодня (кроме первых 10)
    today_orders = Order.objects.filter(order_date__date=timezone.now().date())
    if today_orders.count() > 10:
        today_orders[10:].delete()
    
    update_business_metrics()
    print("   Метрика обновлена. Проверьте в Grafana: статус Resolved")
    
    print("\n" + "=" * 60)
    print("Тестирование завершено!")
    print("=" * 60)

if __name__ == '__main__':
    test_alert_scenarios()
```

---

## 📱 Настройка уведомлений (опционально)

### Email уведомления

1. **Настройте SMTP**:
   - Administration → Notifications
   - Добавьте SMTP сервер

2. **Создайте Contact Point**:
   - Alerting → Contact points
   - Добавьте Email contact point

3. **Создайте Notification Policy**:
   - Alerting → Notification policies
   - Назначьте contact point для вашего alert rule

### Slack уведомления

1. **Создайте Slack webhook**

2. **Добавьте Contact Point**:
   - Type: Slack
   - Webhook URL: ваш Slack webhook

---

## ✅ Чеклист для проверки

- [ ] Alert Rule создана в Grafana
- [ ] Query правильно настроен (`zhevzhik_orders_today`)
- [ ] Condition правильно настроен (`> 50`)
- [ ] For период установлен (5 минут)
- [ ] Annotations добавлены
- [ ] Labels добавлены
- [ ] Панель для мониторинга создана
- [ ] Тестирование Normal состояния
- [ ] Тестирование Pending состояния
- [ ] Тестирование Firing состояния
- [ ] Тестирование Resolved состояния
- [ ] Уведомления настроены (опционально)

---

## 🎯 Готово!

Теперь у вас настроен мониторинг метрики `zhevzhik_orders_today` с Alert Rules в Grafana!

**Резюме:**
- ✅ Метрика: `zhevzhik_orders_today`
- ✅ Alert Rule: "Высокое количество заказов сегодня"
- ✅ Порог: 50 заказов
- ✅ For: 5 минут
- ✅ Статусы: Normal → Pending → Firing → Resolved

**Для демонстрации:**
1. Покажите Normal состояние (10 заказов)
2. Увеличьте до 55 заказов → Pending
3. Подождите 5 минут → Firing
4. Уменьшите до 40 заказов → Resolved

---

## 📚 Дополнительные ресурсы

- [Grafana Alerting Documentation](https://grafana.com/docs/grafana/latest/alerting/)
- [Prometheus Query Language](https://prometheus.io/docs/prometheus/latest/querying/basics/)
- [Alert Rule Best Practices](https://grafana.com/docs/grafana/latest/alerting/alerting-rules/create-alert-rule/)

